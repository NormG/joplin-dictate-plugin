import { createWhisper } from 'joplin-whisper';
import { DictatePlugin } from './DictatePlugin';

export default class DictatePlugin {
  constructor() {
    const whisper = createWhisper();
    this.whisper = whisper;
    // ...
  }

  async start(dictation) {
    await this.whisper.start(dictation);
    // ...
  }
}
